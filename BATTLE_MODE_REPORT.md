# 경쟁 모드(Battle Mode) 설계 보고서

**프로젝트:** PerplexBrainbow (`com.blazar.lex`)  
**작업 기간:** 2026-04-01 ~ 2026-04-07  
**대상 브랜치:** `supabase-migration`

---

## 1. 왜 만들었나

싱글/챌린지 모드는 자기 자신과의 싸움이다. 점수를 올리고 랭킹을 확인하는 구조지만, 상대가 없으면 긴장감의 천장이 낮다. 경쟁 모드는 동일한 퍼즐을 실시간으로 상대와 맞붙게 해서 "내가 못하면 상대가 이긴다"는 즉각적인 압박을 만들기 위해 도입했다.

또한 싱글 모드만으로는 재방문 동기가 약하다. 상대가 있는 대전 구조는 매 판의 결과가 달라지므로 반복 플레이 동기를 자연스럽게 만든다.

---

## 2. 전체 구조

### 2-1. 게임 진행 방식: 체력 줄다리기

**참고:** Clash Royale 엘릭서 트레이드 구조, 격투 게임 체력바 원리

내가 햄버거를 완성하면 상대 게이지가 줄어든다. 상대가 완성하면 내 게이지가 줄어든다. 게이지는 중앙값(50%)에서 시작하며, 한쪽 끝(0% 또는 100%)에 도달하면 즉시 승패가 결정된다.

타이머(45초)가 종료될 때까지 게이지가 끝에 닿지 않으면 그 시점의 게이지 위치로 판정한다.

**왜 이 구조인가:**  
HP를 직접 깎는 방식(내가 맞으면 줄어든다)은 수동적인 피격감이 있다. 줄다리기 방식은 양쪽 모두 동시에 공격자이자 수비자가 되므로 행동 하나하나에 무게감이 생긴다.

### 2-2. 종료 조건: 하이브리드

**참고:** Clash Royale (고정 3분 + 타워 파괴 즉시 종료 구조)

| 조건 | 결과 |
|------|------|
| 게이지가 한쪽 끝(0%)에 도달 | 즉시 승패 결정 |
| 45초 타이머 만료 | 게이지 위치로 판정 |

게이지 즉시 종료만 사용하면 실력 차가 클 때 경기가 지나치게 짧아지고, 열세인 플레이어가 무력감을 느낀다. 타이머만 사용하면 게이지가 주는 긴장감이 반감된다. 두 조건을 조합해 "최대 45초, 압도적 실력 차이 시 조기 종료" 구조로 만들었다.

### 2-3. 타이머: 개인 고정 45초, 충전 없음

**기존 방식의 문제:** 햄버거 완성 시 시간이 충전되는 구조였다. 실력에 따라 각자의 타이머가 다르게 흐르면 서버 판정 기준이 흔들리고 공정성이 무너진다.

**변경 방향:** 시작과 동시에 동일한 45초를 가지며, 충전 없이 소모만 된다. 두 플레이어가 동일한 시간 축 위에서 경쟁한다.

**왜 45초인가:**  
싱글/챌린지 모드 기본값이 30초다. 경쟁 모드는 상대와의 심리전이 추가되므로 조금 더 긴 시간이 필요하다. 60초 이상은 텐션이 풀리는 구간이 생긴다. 45초는 긴장감을 유지하면서 역전 가능성도 남기는 최솟값으로 판단했다.

### 2-4. 속도 가속: 시간이 줄수록 빨라진다

**참고:** 테트리스 레벨 가속 메커니즘, 리듬 게임 후반부 BPM 상승

| 남은 시간 | 쌓기 속도 | 재료 등장 간격 |
|-----------|----------|--------------|
| 45초 (시작) | 0.3s | 0.6s |
| 22초 (절반) | 0.2s | 0.4s |
| 0초 (종료) | 0.1s | 0.2s |

시간 비율에 따라 선형 보간(Lerp)으로 부드럽게 가속된다. 후반으로 갈수록 플레이어의 실수 빈도가 올라가고 심리적 압박이 증가한다. 이는 역전을 노리는 플레이어에게도, 리드를 지키려는 플레이어에게도 동시에 적용되므로 공정하다.

싱글/챌린지 모드는 영향 없음 — `GameManager._type == Battle` 조건 분기로 독립 처리.

---

## 3. 기술 구현

### 3-1. 매칭 시스템

Supabase `battle_rooms` 테이블과 `fn_join_or_create_battle` RPC로 구현했다.

```
클릭 → fn_join_or_create_battle RPC → 빈 방 없으면 생성(player1), 있으면 참가(player2)
→ 2초 간격 폴링 → status가 "ready" 또는 "in_progress"가 되면 매칭 완료
→ TrMatchingSession에 RoomId / IsPlayer1 / BurgerSeed 저장
→ BattleHamburger 씬 로드
```

**외부 매칭 서버 없이 DB 폴링으로 구현한 이유:**  
소규모 동시 접속 환경에서 WebSocket 기반 매칭 서버를 별도 운영하는 것은 비용 대비 과도하다. Supabase RPC + 폴링은 구조가 단순하고 서버리스로 유지보수 부담이 없다.

### 3-2. 실시간 게이지 동기화

NativeWebSocket + Supabase Realtime (`postgres_changes`)으로 구현했다.

```
햄버거 완성/실패 이벤트
→ fn_battle_gauge_update RPC (서버에서 게이지 계산)
→ Supabase Realtime이 battle_rooms 변경을 양측에 브로드캐스트
→ 각 클라이언트가 게이지 UI 즉시 업데이트
```

클라이언트에서 게이지를 직접 계산하지 않고 서버(RPC)에서 계산한 뒤 결과를 수신하는 구조다. 클라이언트 간 수치 불일치를 원천 차단하고 치팅 가능성을 낮춘다.

### 3-3. 씬 구조 분리

기존 `PuzzleHamburger.unity`를 그대로 복제하지 않고 `BattleHamburger.unity`를 별도로 구성했다.

| 항목 | PuzzleHamburger | BattleHamburger |
|------|----------------|----------------|
| 결과 처리 | Result 씬 이동 | 팝업(TrUI_BattleResult) |
| 체력바 | 없음 | TrUI_BattleHPBar |
| 상대 진행 표시 | 없음 | TrUI_BurgerProjectile |
| WebSocket | 없음 | TrBattleManager |
| 타이머 종료 처리 | Result 씬 이동 | 서버 Realtime 대기 |

씬을 합칠 경우 `GameManager._type` 분기가 씬 전체에 퍼지고 WebSocket 생명주기 관리가 복잡해진다. 별도 씬이 명확한 책임 분리를 만든다.

### 3-4. BurgerSeed 동기화

두 플레이어가 동일한 문제를 받아야 공정한 대결이 된다. 매칭 완료 시 `burger_seed` 값을 DB에서 읽어 `TrMatchingSession.BurgerSeed`에 저장하고, BattleHamburger 씬 진입 시 `Random.InitState(seed)`를 재적용한다.

씬 전환 과정에서 Unity의 Random 상태가 리셋될 수 있으므로 씬 로드 후 재적용하는 것이 핵심이다.

---

## 4. UI 퀄리티 개선 (2026-04-07)

### 4-1. 체력바 캐릭터 충돌 연출 — Spring Physics 기반 리팩토링

**배경:** 기존 체력바는 게이지 수치만 표시하는 단순 UI였다. 충돌 포인트에 얼굴이 없고 타격감도 없어 대결 긴장감 전달이 미흡했다.

**구현 내용 (`TrUI_BattleHPBar.cs` 전면 재작성):**

Screen Space Overlay 캔버스에서는 Unity Physics가 동작하지 않으므로 `Update()`에서 직접 스프링 물리를 시뮬레이션했다.

| 항목 | 내용 |
|------|------|
| 스프링 물리 | 매 프레임 Hooke 법칙 + 감쇠(damping) 적용, 충돌 반발력(restitution) 포함 |
| 상시 압력 | `_restingPressure`로 항상 서로를 밀어내어 중앙에서 자연스럽게 맞닿아 흔들리는 느낌 |
| 기울기 연출 | 속도에 비례한 Z축 회전 (`_tiltFactor`) — 이동 방향으로 기울었다가 복원 |
| 표정 시스템 | Neutral/Winning/Losing 3단계. 게이지 비율에 따라 자동 교체. 상대 얼굴은 `localScale.x = -1` 수평 반전 적용 |
| 구조 | `CollisionAnchor` 빈 오브젝트 아래 MyFace(X:-35), OppFace(X:+35) 배치 — 이미지 크기만큼 떨어져 맞닿은 느낌 |
| 기원점 캐싱 | `_myFaceOrigin` / `_oppFaceOrigin`을 `Start()`에서 캐시. 리셋 시 항상 이 값으로 복원 |

**달러 이펙트 (`TrUI_BattleEffects.cs` 신규 생성):**

정답 시 충돌 포인트에서 `$` 텍스트가 방사형으로 튀어오르는 오브젝트 풀 방식 이펙트.
- `zPlayAt()` — 정답 시 위 반원(30°~150°) 버스트
- `zPlayCollisionBurst()` — 게임 시작 충돌 시 360° 전방향 버스트

### 4-2. 게임 시작 인트로 애니메이션

게임 시작 신호(`yWaitBattleStartSignal`) 수신 시 `zPlayIntroAnimation()` 호출:

1. 양쪽 얼굴이 화면 바깥(`±_introStartOffset = 300f`)에서 출발
2. 0.55초 동안 `InCubic` 이징으로 중앙으로 돌진
3. 충돌 지점 도달 시 PunchScale + 360° 달러 버스트 + 화면 셰이크
4. 이후 스프링 물리 활성화(`_physicsEnabled = true`)

`_physicsEnabled` 플래그로 인트로 트윈 중에는 스프링 물리가 개입하지 않도록 처리.

### 4-3. 시간 압박 게임 패킹 속도 연동

햄버거 포장(`yTakeOut`) 애니메이션이 남은 시간과 무관하게 고정 속도로 재생되던 문제 수정.

```
speedRatio = _viewBurgerSpeed / _baseViewBurgerSpeed
포장 대기 시간 = 0.3s × speedRatio
스케일 애니메이션 = 0.5s × speedRatio
```

시간이 촉박할수록 포장 연출도 빠르게 진행되어 게임 페이스와 일관성이 생겼다.

### 4-4. 음악 피치 가속 (시간 압박 강조)

남은 시간 비율에 따라 음악 속도(피치)를 실시간 조절:

| 남은 시간 | 피치 |
|-----------|------|
| 45초 (시작) | 1.0 (보통) |
| 0초 (종료) | 1.3 (30% 빠름) |

`TrAudio_Music.zzSetPitch(float)` 신규 메서드 추가. `_audioSource`, `_head`, `_body` AudioSource 세 곳 동시 적용.  
`TrAudio_.zStopMusic()` 에 `_audioSource.pitch = 1.0f` 리셋 추가 — 게임 종료 후 다른 씬에서 음악이 빠른 속도로 재생되는 문제 방지.

---

## 5. 버그 수정 이력 (2026-04-06)

이번 세션에서 발견 및 수정된 주요 이슈:

| 분류 | 내용 | 파일 |
|------|------|------|
| P0 | 타이머 종료 시 Battle 모드임에도 Result 씬으로 이탈 | `TrUI_PuzzlePause.cs` |
| P0 | `PendingRematch` 소비 코드 없음 → 리매치 동작 안 함 | `TrUI_MatchingPopup.cs` |
| P0 | `ySubscribePuzzleEvents()` 실패 시 재시도 없음 | `TrBattleManager.cs` |
| P0 | `OnBattleEnded` 중복 발화 방어 없음 | `TrBattleManager.cs` |
| P0 | `GameNotice` 오브젝트 비활성으로 챌린지 모드 시작 불가 | `PuzzleHamburger.unity` |
| P1 | 로비에 `TrUI_MatchingPopup` 중복 인스턴스 | `LobbyHamburger.unity` |
| P1 | QuitWindow `_hidePos == _showPos` → 슬라이드 연출 없음 | `BattleHamburger.unity` |
| P1 | Disconnect 비동기 완료 전 씬 전환 | `TrUI_BattleResult.cs` |
| P2 | `zSignalBattleStart()` 호출 누락 → DB 상태 전환 불가 | `TrBattleManager.cs` |
| P2 | BurgerSeed 씬 전환 후 유실 | `TrMatchingSession.cs` |

---

## 6. 더 손봐야 할 것

### P0 — 다음 단계 필수

**게이지 0 즉시 종료 판정 (서버 연동)**  
현재 클라이언트에서 게이지 비율이 0/100에 도달하면 결과창을 표시하지만, 서버(fn_battle_gauge_update)에서 status를 "finished"로 변경하는 조건 로직이 보완되어야 한다. 클라이언트와 서버 판정이 동시에 일관되게 동작해야 한다.

**화면 분할 UI 구현**  
현재 결과창(TrUI_BattleResult)은 구현됐으나, 게임 중 내 화면과 상대 진행 상황을 동시에 보여주는 분할 UI가 미완성이다.

### P1 — 안정화 필요

**타이머 서버 기준 동기화**  
현재 타이머는 클라이언트 각자에서 독립적으로 흐른다. 네트워크 지연이 심한 환경에서 두 플레이어의 종료 시점이 수 초 차이 날 수 있다. battle_rooms에 `started_at` 타임스탬프를 저장하고 두 클라이언트가 동일한 기준으로 계산하는 방식으로 개선이 필요하다.

**밸런싱: 게이지 이동량**  
햄버거 1개 완성 시 게이지가 얼마나 이동하는지 수치가 확정되지 않았다. 난이도별(재료 수, 조합 복잡도)로 이동량을 다르게 적용하는 밸런싱이 필요하다.

**리매치 루프 UX**  
리매치 버튼 → 로비 복귀 → 자동 매칭 팝업 열림 흐름은 구현됐으나, 실기기에서 전체 루프 검증이 필요하다.

### P2 — 이후 개선

**봇 매칭 (대기 플레이어 없을 때)**  
매칭 타임아웃(30초) 후 상대를 못 찾으면 단순 안내만 표시한다. 향후 봇 상대를 붙여서 혼자서도 연습 가능한 구조를 추가할 수 있다.

**랭킹 연동**  
현재 경쟁 모드 승/패 기록이 DB에 저장되지 않는다. `battle_results` 테이블을 추가해 승률, 연승 기록 등 경쟁 모드 전용 랭킹을 구성할 수 있다.

---

## 요약

| 구분 | 항목 | 시점 | 상태 |
|------|------|------|------|
| P0 | 게이지 0 즉시 종료 서버 판정 연동 | 다음 세션 | 미완 |
| P0 | 화면 분할 UI 구현 | 다음 세션 | 미완 |
| P1 | 타이머 서버 기준 동기화 | 안정화 단계 | 미완 |
| P1 | 게이지 이동량 밸런싱 | 안정화 단계 | 미완 |
| P1 | 체력바 Spring Physics 충돌 연출 | 2026-04-07 완료 | ✅ |
| P1 | 게임 시작 인트로 애니메이션 | 2026-04-07 완료 | ✅ |
| P1 | 포장 속도 시간 압박 연동 | 2026-04-07 완료 | ✅ |
| P1 | 음악 피치 가속 (시간 압박) | 2026-04-07 완료 | ✅ |
| P2 | 봇 매칭 | 출시 이후 | 미완 |
| P2 | 경쟁 모드 전용 랭킹 | 출시 이후 | 미완 |
